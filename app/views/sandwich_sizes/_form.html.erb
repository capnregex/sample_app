<%= form_for(@sandwich_size) do |f| %>
  <% if @sandwich_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sandwich_size.errors.count, "error") %> prohibited this sandwich_size from being saved:</h2>

      <ul>
      <% @sandwich_size.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
